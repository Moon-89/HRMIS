{
    "version": 2,
    "framework": "create-react-app",
    "buildCommand": "CI=false npm run build",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}