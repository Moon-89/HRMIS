# Copy this file to .env and update values for your environment
# The frontend will send requests to REACT_APP_API_URL + requestPath
# Example: REACT_APP_API_URL=http://localhost:4000/api

REACT_APP_API_URL=http://localhost:4000
# If your backend is on the same origin (same host:port) you can leave this empty
# REACT_APP_API_URL=

# Alternatively, during development you can add a proxy to package.json:
# "proxy": "http://localhost:4000"
